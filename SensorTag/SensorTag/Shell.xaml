<Page
    x:Class="SensorTag.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SensorTag"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:SensorTag.Views"
    mc:Ignorable="d">

    <SplitView Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <SplitView.Pane>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <RadioButton Command="{Binding NavCommand}" Content="Home" GroupName="Nav">
                    <RadioButton.CommandParameter>
                        <views:ConfigPage/>
                    </RadioButton.CommandParameter>
                </RadioButton>
            </Grid>            
        </SplitView.Pane>
    </SplitView>
</Page>
